# Docker Compose PostgreSQL defaults and MatchStream's local database URL.
POSTGRES_DB=matchstream
POSTGRES_USER=matchstream
POSTGRES_PASSWORD=matchstream
MATCHSTREAM_DATABASE_URL=postgresql://matchstream:matchstream@localhost:5432/matchstream
